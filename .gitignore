node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Supabase CLI's local project-link cache (pooler URL, project ref, etc.) —
# machine-specific, regenerated by `supabase link`, never meant to be shared.
supabase/.temp/

# Unrelated nested project directories that happen to live under this
# folder's own .claude/worktrees — not part of Cabadra.
.claude/worktrees/
